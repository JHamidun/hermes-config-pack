# Orchestrator

> Add your orchestrator configuration here.

The orchestrator coordinates multi-agent workflows. See `agents/orchestrator.md`
for the agent definition and `rules/scaling.md` for the decision tree.
