# gstack — откуда это

Источник: **gstack** (Garry Tan) — см. `package.json` → `repository`,
`README.md` и `CHANGELOG.md` внутри каталога.

Лицензия: **MIT**, `Copyright (c) 2026 Garry Tan`; полный текст — в файле
`LICENSE` рядом. MIT требует, чтобы этот текст сопровождал любую копию, включая
копию внутри плагина `plugins/browser-testing/skills/gstack/`.

Изменено относительно первоисточника: интеграция в маршрутизацию пака, сборка
бинаря `browse` вынесена в `setup_runtime.py`. Собранные артефакты
(`dist/`, `*.exe`) в раздачу не входят — ученик собирает их у себя.
